{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd packages/server && bunx prisma migrate resolve --rolled-back 20251213164335_refine_schema && bunx prisma migrate resolve --rolled-back 20251213153442_init && bunx prisma migrate resolve --rolled-back 20251217133813_change_price_to_decimal && cd ../.. && bun run migrate && bun run start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

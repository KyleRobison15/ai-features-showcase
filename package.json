{
  "name": "ai-features-showcase",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun run build:client && bun run build:server",
    "build:client": "cd packages/client && bun install && bun run build",
    "build:server": "cd packages/server && bun install && bunx prisma generate",
    "migrate": "cd packages/server && bunx prisma migrate deploy",
    "start": "cd packages/server && bun run start",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ]
}
